module.exports = {
    addMember: async ( req, res ) => {
        const db = req.app.get('db')
        if( req.session.user ) {
            
            
        }
    },
    getMembers: async ( req, res ) => {
        const db = req.app.get('db')
        if( req.session.user ) {
            
        }
    },
    deleteMember: async ( req, res ) => {
        const db = req.app.get('db')
        if( req.session.user ) {
            
        }
    }
}